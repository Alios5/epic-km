# Dependencies — installed via `npm install`
node_modules

# Frontend build output (regenerated by `npm run build` / `tauri dev`)
/build
/.svelte-kit
/package

# Rust build artifacts (recompiled by cargo on `tauri dev`)
target/

# Env files
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*

# OS / editor cruft
.DS_Store
Thumbs.db

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
